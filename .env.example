# Application Configuration
APP_NAME=ZATCA Compliance API
APP_VERSION=1.0.0
DEBUG=false

# Swagger / OpenAPI docs (SaaS best practice: disable in production)
# ENABLE_DOCS: optional; set true|false to override. If unset, derived from ENVIRONMENT_NAME (local|dev -> true, prod -> false).
# ENVIRONMENT_NAME: local | dev | production. Use production for prod; local/dev enable docs by default.
ENVIRONMENT_NAME=local
# DOCS_LOCALHOST_ONLY: when true, /docs and /openapi.json only from 127.0.0.1 / ::1 (recommended)
DOCS_LOCALHOST_ONLY=true

# API Security
API_KEY_HEADER=X-API-Key
API_KEYS=your-api-key-1,your-api-key-2

# ZATCA Environment (sandbox | production)
ENVIRONMENT=sandbox

# ZATCA Integration
ZATCA_SANDBOX_BASE_URL=https://gw-apic-gov.gazt.gov.sa/e-invoicing/developer-portal
ZATCA_PRODUCTION_BASE_URL=https://gw-apic-gov.gazt.gov.sa/e-invoicing/core
ZATCA_TIMEOUT=30

# Cryptographic
SIGNING_KEY_PATH=/path/to/private-key.pem
SIGNING_CERTIFICATE_PATH=/path/to/certificate.pem

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
